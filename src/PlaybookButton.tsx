import React from 'react'

export default function PlaybookButton(props: React.DetailedHTMLProps<React.ButtonHTMLAttributes<HTMLButtonElement>, HTMLButtonElement>) {
	return <button className='playbook__button' {...props} />
}